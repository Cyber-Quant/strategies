diff --git a/pages/backtest.py b/pages/backtest.py
index cab4a3c..d20200f 100644
--- a/pages/backtest.py
+++ b/pages/backtest.py
@@ -13,6 +13,7 @@ from strategies.boll import BOLL, BOLLBacktest, BOLLInfo
 from strategies.bottom_break_up import BottomBreakUp, BottomBreakUpBacktest, \
     BottomBreakUpInfo
 from strategies.kdj import KDJ, KDJBacktest, KDJInfo
+from strategies.kitten import Kitten, KittenBacktest, KittenInfo
 from strategies.lucky_duck_head import LuckyDuckHead, LuckyDuckHeadBacktest, \
     LuckyDuckHeadInfo
 from strategies.macd import MACD, MACDBacktest, MACDInfo
@@ -235,6 +236,7 @@ class Backtest(QWidget):
         boll_info = BOLLInfo()
         bottom_break_up_info = BottomBreakUpInfo()
         kdj_info = KDJInfo()
+        kitten_info = KittenInfo()
         lucky_duck_head_info = LuckyDuckHeadInfo()
         macd_info = MACDInfo()
         rsi_info = RSIInfo()
@@ -251,6 +253,10 @@ class Backtest(QWidget):
             self.backtest_thread = KDJBacktest(stocks, s_date, e_date,
                                                init_money, fee,
                                                pass_fee, tax)
+        elif self.current_strategy_name == kitten_info.name:
+            self.backtest_thread = KittenBacktest(stocks, s_date, e_date,
+                                                  init_money, fee,
+                                                  pass_fee, tax)
         elif self.current_strategy_name == lucky_duck_head_info.name:
             self.backtest_thread = LuckyDuckHeadBacktest(stocks, s_date, e_date,
                                                          init_money, fee,
@@ -321,6 +327,7 @@ class Backtest(QWidget):
         boll_info = BOLLInfo()
         bottom_break_up_info = BottomBreakUpInfo()
         kdj_info = KDJInfo()
+        kitten_info = KittenInfo()
         lucky_duck_head_info = LuckyDuckHeadInfo()
         macd_info = MACDInfo()
         rsi_info = RSIInfo()
@@ -331,6 +338,8 @@ class Backtest(QWidget):
             backtest = BottomBreakUp()
         elif self.current_strategy_name == kdj_info.name:
             backtest = KDJ()
+        elif self.current_strategy_name == kitten_info.name:
+            backtest = Kitten()
         elif self.current_strategy_name == lucky_duck_head_info.name:
             backtest = LuckyDuckHead()
         elif self.current_strategy_name == macd_info.name:
diff --git a/pages/choose.py b/pages/choose.py
index ce1e32a..d0a813d 100644
--- a/pages/choose.py
+++ b/pages/choose.py
@@ -11,6 +11,7 @@ from strategies.boll import BOLLChoose, BOLLInfo
 from strategies.bottom_break_up import BottomBreakUpChoose, BottomBreakUpInfo
 from strategies.common import get_stat_date, get_value_info
 from strategies.kdj import KDJChoose, KDJInfo
+from strategies.kitten import KittenChoose, KittenInfo
 from strategies.lucky_duck_head import LuckyDuckHeadChoose, LuckyDuckHeadInfo
 from strategies.macd import MACDChoose, MACDInfo
 from strategies.mcst import MCSTInfo
@@ -48,6 +49,7 @@ class Choose(Plots):
         self.boll_info = BOLLInfo()
         self.bottom_break_up_info = BottomBreakUpInfo()
         self.kdj_info = KDJInfo()
+        self.kitten_info = KittenInfo()
         self.lucky_duck_head_info = LuckyDuckHeadInfo()
         self.macd_info = MACDInfo()
         self.mcst_info = MCSTInfo()
@@ -322,6 +324,8 @@ class Choose(Plots):
                         self.stocks_to_be_chosen)
                 elif self.apply_strategies[0] == self.kdj_info.name:
                     self.choose_thread = KDJChoose(self.stocks_to_be_chosen)
+                elif self.apply_strategies[0] == self.kitten_info.name:
+                    self.choose_thread = KittenChoose(self.stocks_to_be_chosen)
                 elif self.apply_strategies[0] == self.lucky_duck_head_info.name:
                     self.choose_thread = LuckyDuckHeadChoose(
                         self.stocks_to_be_chosen)
diff --git a/pages/nav.py b/pages/nav.py
index 20f1667..436d839 100644
--- a/pages/nav.py
+++ b/pages/nav.py
@@ -14,6 +14,7 @@ from pages.watch import Watch
 from strategies.boll import BOLLConfig, BOLLInfo
 from strategies.bottom_break_up import BottomBreakUpConfig, BottomBreakUpInfo
 from strategies.kdj import KDJConfig, KDJInfo
+from strategies.kitten import KittenConfig, KittenInfo
 from strategies.lucky_duck_head import LuckyDuckHeadConfig, LuckyDuckHeadInfo
 from strategies.macd import MACDConfig, MACDInfo
 from strategies.mcst import MCSTInfo
@@ -76,6 +77,7 @@ class Nav(QWidget):
         self.lucky_duck_head_info = LuckyDuckHeadInfo()
         self.mcst_info = MCSTInfo()
         self.bottom_break_up_info = BottomBreakUpInfo()
+        self.kitten_info = KittenInfo()
         self.wr_info = WRInfo()
         self.boll_info = BOLLInfo()
         self.macd_info = MACDInfo()
@@ -117,6 +119,11 @@ class Nav(QWidget):
                 'choose': 'Y' if self.percent_change_info.choose_flag else 'N',
                 'watch': 'Y' if self.percent_change_info.watch_flag else 'N'
             },
+            {
+                'name': self.kitten_info.name,
+                'choose': 'Y' if self.kitten_info.choose_flag else 'N',
+                'watch': 'Y' if self.kitten_info.watch_flag else 'N'
+            },
             {
                 'name': self.boll_info.name,
                 'choose': 'Y' if self.boll_info.choose_flag else 'N',
@@ -350,6 +357,10 @@ class Nav(QWidget):
             cfg_dlg = BottomBreakUpConfig(self)
             cfg_dlg.show()
             cfg_dlg.exec_()
+        if name == self.kitten_info.name:
+            cfg_dlg = KittenConfig(self)
+            cfg_dlg.show()
+            cfg_dlg.exec_()
         if name == self.wr_info.name:
             cfg_dlg = WRConfig(self)
             cfg_dlg.show()
diff --git a/widgets/plots.py b/widgets/plots.py
index 32df34a..38dd38f 100644
--- a/widgets/plots.py
+++ b/widgets/plots.py
@@ -7,6 +7,7 @@ from strategies.common import get_latest_batch_data
 from strategies.boll import BOLL
 from strategies.bottom_break_up import BottomBreakUp
 from strategies.kdj import KDJ
+from strategies.kitten import Kitten
 from strategies.lucky_duck_head import LuckyDuckHead
 from strategies.macd import MACD
 from strategies.mcst import MCST
@@ -474,6 +475,13 @@ class Plots(QWidget):
             mav_10 = bottom_break_up.calc_mav(volumes, 10)
             vol_data = [mav_5, mav_10]
             vol_pen_colors = ['r', 'y']
+        elif self.current_indicator_name == self.kitten_info.name:
+            kitten = Kitten()
+            ma = kitten.calc_batch_ma_indicator(closes)
+            k_data = [ma]
+            k_pen_colors = ['y']
+            vol_data = []
+            vol_pen_colors = []
         elif self.current_indicator_name == self.lucky_duck_head_info.name:
             lucky_duck_head = LuckyDuckHead()
             fast_ma = lucky_duck_head.calc_fast_ma(closes)
